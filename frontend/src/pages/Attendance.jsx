import React,{useEffect,useState} from 'react';
import { Clock, MapPin } from 'lucide-react';
import api from '../api';
export default function Attendance(){ const [rows,setRows]=useState([]); const load=()=>api.get('/api/attendance').then(r=>setRows(r.data)); useEffect(load,[]); const punch=async()=>{await api.post('/api/attendance/punch',{mode:'Office',source:'Web'}); load();}; return <section className="page"><div className="page-head"><div><h1>Attendance Control</h1><p>Office, WFH and biometric-ready attendance tracking.</p></div><button className="primary" onClick={punch}><Clock size={17}/> Punch In / Out</button></div><div className="table-card"><table><thead><tr><th>Employee</th><th>Date</th><th>Punch In</th><th>Punch Out</th><th>Mode</th><th>Status</th></tr></thead><tbody>{rows.map(r=><tr key={r._id}><td>{r.employeeName}</td><td>{r.date}</td><td>{r.punchIn}</td><td>{r.punchOut||'Working'}</td><td><MapPin size={14}/> {r.mode}</td><td><span className="badge">{r.status}</span></td></tr>)}</tbody></table></div></section> }

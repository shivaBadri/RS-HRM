import React,{useEffect,useState} from 'react';
import api from '../api';
export default function Leaves(){ const [rows,setRows]=useState([]); const [form,setForm]=useState({leaveType:'Casual Leave',fromDate:'',toDate:'',reason:''}); const load=()=>api.get('/api/leaves').then(r=>setRows(r.data)); useEffect(load,[]); const apply=async e=>{e.preventDefault(); await api.post('/api/leaves',form); setForm({...form,reason:''}); load();}; return <section className="page"><div className="page-head"><div><h1>Leave Management</h1><p>Apply, approve and track employee leave requests.</p></div></div><div className="grid-2"><form className="panel form" onSubmit={apply}><h3>Apply Leave</h3><select value={form.leaveType} onChange={e=>setForm({...form,leaveType:e.target.value})}><option>Casual Leave</option><option>Sick Leave</option><option>WFH</option><option>Paid Leave</option></select><input type="date" value={form.fromDate} onChange={e=>setForm({...form,fromDate:e.target.value})}/><input type="date" value={form.toDate} onChange={e=>setForm({...form,toDate:e.target.value})}/><textarea placeholder="Reason" value={form.reason} onChange={e=>setForm({...form,reason:e.target.value})}/><button className="primary">Submit Request</button></form><div className="panel"><h3>Requests</h3><div className="list">{rows.map(l=><div className="list-row" key={l._id}><b>{l.employeeName}</b><span>{l.leaveType} • {l.fromDate} to {l.toDate} • {l.status}</span></div>)}</div></div></div></section> }
